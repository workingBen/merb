scope 'merb-gen' do
  #TODO: Generators have not been ported to Thor yet.
  # dir = File.join(File.dirname(__FILE__), 'lib', 'generators/')
  # Merb.add_generators dir + 'base', dir + 'full', dir + 'thin', dir + 'very_thin'
end
